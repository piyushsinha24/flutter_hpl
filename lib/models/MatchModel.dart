class MatchModel {
  String firstTeam, secondTeam, firstScore, secondScore, matchId;
  int status;

  MatchModel(this.firstTeam, this.secondTeam, this.firstScore, this.secondScore,
      this.matchId, this.status);
}
